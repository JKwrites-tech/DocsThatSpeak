---
id: no-one-reads-from-the-top
title: No One Reads Your Docs From the Top
sidebar_position: 3
---

import React from 'react';

export function ReadingPhasesDiagram() {
  const phases = [
    {
      id: 'finding',
      label: 'Finding',
      num: '01',
      bg: '#E6F1FB',
      border: '#B5D4F4',
      labelColor: '#0C447C',
      time: 'About 10 seconds',
      question: 'Is what I need even on this page?',
      matters: ['Heading structure', 'Page labels and titles', 'Navigation and search', 'Visual layout'],
      reach: '100% of users',
      reachBg: '#042C53',
      reachColor: '#B5D4F4',
      note: 'Prose quality is irrelevant at this stage. If the structure does not signal the right content, the user leaves.',
    },
    {
      id: 'scanning',
      label: 'Scanning',
      num: '02',
      bg: '#FAEEDA',
      border: '#FAC775',
      labelColor: '#633806',
      time: 'A few seconds',
      question: 'Does this section look like the right one?',
      matters: ['First sentence of each paragraph', 'Code snippets and examples', 'Warning and note callouts', 'Bold terms and keywords'],
      reach: 'Users who passed Finding',
      reachBg: '#633806',
      reachColor: '#FAC775',
      note: 'If a section passes this scan, the user reads it. If it does not pass, they go back to Finding.',
    },
    {
      id: 'reading',
      label: 'Reading',
      num: '03',
      bg: '#EAF3DE',
      border: '#C0DD97',
      labelColor: '#27500A',
      time: 'Extended attention',
      question: 'Is this accurate, clear, and complete?',
      matters: ['Prose clarity and flow', 'Logical step order', 'Accuracy of every detail', 'Complete and unambiguous steps'],
      reach: 'A minority of users',
      reachBg: '#27500A',
      reachColor: '#C0DD97',
      note: 'This is the phase most documentation is written for. It is also the phase most users never reach.',
    },
  ];

  return (
    <div style={{ margin: '1.5rem 0 2rem' }}>
      <div style={{
        fontSize: '11px',
        fontFamily: 'monospace',
        letterSpacing: '0.08em',
        textTransform: 'uppercase',
        color: 'var(--ifm-color-emphasis-500)',
        marginBottom: '0.75rem',
      }}>
        The three reading phases
      </div>
      <div style={{
        display: 'grid',
        gridTemplateColumns: 'repeat(auto-fit, minmax(200px, 1fr))',
        gap: '0',
        alignItems: 'stretch',
      }}>
        {phases.map((phase, i) => (
          <div key={phase.id} style={{ display: 'flex', alignItems: 'stretch' }}>
            <div style={{
              flex: 1,
              background: phase.bg,
              border: `1px solid ${phase.border}`,
              borderRadius: i === 0 ? '12px 0 0 12px' : i === phases.length - 1 ? '0 12px 12px 0' : '0',
              padding: '1rem 1.1rem',
              display: 'flex',
              flexDirection: 'column',
              gap: '0.5rem',
            }}>
              <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}>
                <span style={{
                  fontFamily: 'monospace',
                  fontSize: '10px',
                  fontWeight: 500,
                  color: phase.labelColor,
                  opacity: 0.6,
                }}>{phase.num}</span>
                <span style={{
                  background: phase.reachBg,
                  color: phase.reachColor,
                  fontSize: '10px',
                  fontFamily: 'monospace',
                  padding: '2px 7px',
                  borderRadius: '4px',
                  letterSpacing: '0.04em',
                }}>{phase.reach}</span>
              </div>
              <div style={{ fontSize: '16px', fontWeight: 500, color: phase.labelColor }}>{phase.label}</div>
              <div style={{ fontSize: '11px', color: phase.labelColor, opacity: 0.75, lineHeight: 1.4 }}>{phase.time}</div>
              <div style={{
                fontSize: '12px',
                fontStyle: 'italic',
                color: phase.labelColor,
                lineHeight: 1.5,
                paddingBottom: '0.5rem',
                borderBottom: `0.5px solid ${phase.border}`,
              }}>"{phase.question}"</div>
              <div style={{ fontSize: '11px', fontFamily: 'monospace', textTransform: 'uppercase', letterSpacing: '0.06em', color: phase.labelColor, opacity: 0.6 }}>What matters here</div>
              <ul style={{ margin: 0, padding: '0 0 0 1rem' }}>
                {phase.matters.map((m, j) => (
                  <li key={j} style={{ fontSize: '12px', color: phase.labelColor, lineHeight: 1.65 }}>{m}</li>
                ))}
              </ul>
              <div style={{
                marginTop: 'auto',
                paddingTop: '0.6rem',
                fontSize: '11px',
                color: phase.labelColor,
                lineHeight: 1.55,
                opacity: 0.8,
                borderTop: `0.5px solid ${phase.border}`,
              }}>{phase.note}</div>
            </div>
            {i < phases.length - 1 && (
              <div style={{
                display: 'flex',
                alignItems: 'center',
                padding: '0 2px',
                background: 'transparent',
                zIndex: 1,
              }}>
                <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
                  <path d="M4 8h8M9 5l3 3-3 3" stroke="var(--ifm-color-emphasis-400)" strokeWidth="1.2" strokeLinecap="round" strokeLinejoin="round"/>
                </svg>
              </div>
            )}
          </div>
        ))}
      </div>
    </div>
  );
}

export function EntryPointDiagram() {
  const steps = [
    { num: 1, label: 'Introduction', muted: true },
    { num: 2, label: 'Prerequisites', muted: true },
    { num: 3, label: 'Step 1: Install the package', muted: true },
    { num: 4, label: 'Step 4: Configure the connection', highlight: true },
    { num: 5, label: 'Step 5: Test the setup', muted: true },
    { num: 6, label: 'Step 6: Handle errors', muted: true },
    { num: 7, label: 'Troubleshooting', muted: true },
  ];

  const sources = [
    { label: 'Search result', icon: '🔍' },
    { label: 'Slack message', icon: '💬' },
    { label: 'Product tooltip', icon: '🔗' },
  ];

  return (
    <div style={{ margin: '1.5rem 0 2rem' }}>
      <div style={{
        fontSize: '11px',
        fontFamily: 'monospace',
        letterSpacing: '0.08em',
        textTransform: 'uppercase',
        color: 'var(--ifm-color-emphasis-500)',
        marginBottom: '0.75rem',
      }}>
        Where users actually land
      </div>
      <div style={{
        display: 'grid',
        gridTemplateColumns: '1fr auto 1fr',
        gap: '0 1rem',
        alignItems: 'center',
        background: 'var(--ifm-color-emphasis-100)',
        border: '0.5px solid var(--ifm-color-emphasis-300)',
        borderRadius: '12px',
        padding: '1.25rem',
      }}>
        <div style={{ display: 'flex', flexDirection: 'column', gap: '6px' }}>
          {steps.map((step) => (
            <div key={step.num} style={{
              background: step.highlight ? '#042C53' : 'var(--ifm-color-emphasis-0, #fff)',
              border: step.highlight ? '1.5px solid #185FA5' : '0.5px solid var(--ifm-color-emphasis-300)',
              borderRadius: '8px',
              padding: '7px 12px',
              fontSize: '12px',
              fontWeight: step.highlight ? 500 : 400,
              color: step.highlight ? '#fff' : 'var(--ifm-color-emphasis-500)',
              opacity: step.muted ? 0.5 : 1,
              display: 'flex',
              alignItems: 'center',
              gap: '8px',
            }}>
              <span style={{
                fontFamily: 'monospace',
                fontSize: '10px',
                opacity: 0.6,
                minWidth: '14px',
              }}>{step.num}</span>
              {step.label}
              {step.highlight && (
                <span style={{
                  marginLeft: 'auto',
                  fontSize: '10px',
                  background: '#185FA5',
                  color: '#B5D4F4',
                  padding: '2px 6px',
                  borderRadius: '4px',
                  fontFamily: 'monospace',
                  letterSpacing: '0.04em',
                  whiteSpace: 'nowrap',
                }}>user lands here</span>
              )}
            </div>
          ))}
        </div>

        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: '4px' }}>
          {sources.map((src, i) => (
            <React.Fragment key={i}>
              <div style={{ fontSize: '11px', color: 'var(--ifm-color-emphasis-600)', whiteSpace: 'nowrap', opacity: 0.7 }}>
                {src.icon}
              </div>
              {i < sources.length - 1 && (
                <div style={{ height: '14px', width: '0.5px', background: 'var(--ifm-color-emphasis-300)' }} />
              )}
            </React.Fragment>
          ))}
          <div style={{ height: '8px', width: '0.5px', background: 'var(--ifm-color-emphasis-300)' }} />
          <svg width="14" height="14" viewBox="0 0 14 14" fill="none">
            <path d="M2 7h10M8 4l3 3-3 3" stroke="var(--ifm-color-emphasis-500)" strokeWidth="1.2" strokeLinecap="round" strokeLinejoin="round"/>
          </svg>
        </div>

        <div style={{ display: 'flex', flexDirection: 'column', gap: '8px' }}>
          {sources.map((src, i) => (
            <div key={i} style={{
              background: 'var(--ifm-color-emphasis-0, #fff)',
              border: '0.5px solid var(--ifm-color-emphasis-300)',
              borderRadius: '8px',
              padding: '8px 12px',
              fontSize: '12px',
              color: 'var(--ifm-color-emphasis-700)',
              display: 'flex',
              alignItems: 'center',
              gap: '8px',
            }}>
              <span>{src.icon}</span>
              {src.label}
            </div>
          ))}
          <div style={{
            fontSize: '11px',
            color: 'var(--ifm-color-emphasis-500)',
            lineHeight: 1.55,
            paddingTop: '0.4rem',
            borderTop: '0.5px solid var(--ifm-color-emphasis-300)',
          }}>
            None of these users read steps 1 through 3. They are already at step 4 and need to understand it on its own.
          </div>
        </div>
      </div>
    </div>
  );
}

# No One Reads Your Docs From the Top

Here is something most writers know but few actually write for: no one reads documentation the way it was written.

Writers compose from top to bottom, context first, then procedure, then notes. Users arrive from the middle. A search result, a link in a tooltip, a colleague's Slack message. They land on step 4 with no idea what happened in steps 1 through 3. They scan for the thing they need, skim the paragraph around it, and leave.

Documentation written for a reader who starts at the top and reads to the bottom is documentation optimised for almost no one.

---

## The three ways people read

Research in technical communication and UX has consistently identified three reading patterns that users apply to documentation, often moving through all three within a single article.

<ReadingPhasesDiagram />

Most documentation is written almost entirely for the reading phase. Most users never reach it.

---

## Designing for the user who arrives in the middle

The practical implication of how users actually read is that every section of every article needs to be self-sufficient, not just the article as a whole.

<EntryPointDiagram />

A user landing on step 4 of a 7-step procedure should be able to understand what step 4 is asking them to do without having read the steps before it. This does not mean repeating every prior step. It means writing each step with enough context that a reader encountering it for the first time can orient themselves quickly.

---

## Four techniques that make documentation work for scanners

**Lead with the outcome, not the action.**
*"To enable two-factor authentication, navigate to Settings > Security"* tells the reader what they are about to accomplish before asking them to do anything. This lets them confirm they are in the right place before they act. A reader who is not sure they are on the right page will hesitate, go back, or raise a ticket.

**Use headings as answers, not labels.**
A heading that says *"Authentication"* is a label. A heading that says *"How to set up two-factor authentication"* is an answer to a likely reader question. When users are in the finding phase, they are looking for the heading that matches the question in their head. Answer-style headings match far more often because they use the same language the user is thinking in.

**Front-load every paragraph.**
The most important information belongs in the first sentence of every paragraph, not the last. Users who are scanning will read the first sentence and skip to the next heading. If the key information is buried in sentence four, those users will miss it entirely.

**Make warnings impossible to overlook.**
A warning about data loss or an action that cannot be undone, buried in the third paragraph of a procedure, is a documentation failure waiting to happen. If a step has a consequence the user needs to know before they act, that consequence goes before the step, not after it.

---

## The scannability test

Before publishing any documentation, apply this test.

Cover the body text of every section and read only the headings. Can you follow the structure and main purpose of the article from headings alone?

Then uncover the body text and read only the first sentence of each paragraph. Does the article still make sense?

If the answer to both is yes, you have written documentation that works for users who scan. If the answer is no, you have written documentation that works only for users who read every word, and those users are the minority.

---

## Why this matters more now than it did before

As AI search surfaces direct answers rather than links to articles, and as in-product help delivers short, context-specific snippets rather than full documents, documentation that works non-linearly is becoming more important, not less.

Content structured for linear reading cannot be easily split up, surfaced at the right moment, or understood out of sequence. Content that is modular, self-sufficient, and heading-led can be delivered anywhere, in any order, to any user, and it will still make sense.

Writing for how people actually read is not just good practice. It is the foundation of documentation that works in a world where the user rarely, if ever, starts at the top.
